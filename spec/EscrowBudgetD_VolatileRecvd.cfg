\* Receiver dedup NOT durable: a crash loses recvd while the credited balance is durable, so a
\* retransmit re-credits. Expected: SafetyLe VIOLATED (receiver-side double credit across a crash).
SPECIFICATION Spec
CONSTANTS
  Replicas = {r1, r2}
  CAP = 2
  Tids = {t1}
  Amounts = {1, 2}
  DebitDurableBeforeSend = TRUE
  RecvdDurable = FALSE
INVARIANT SafetyLe
