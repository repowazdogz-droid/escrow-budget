\* Receiver-side idempotency OFF (a retained message can be re-delivered and RE-CREDITS),
\* sender-side ON. Expected: SafetyLe (and Safety) VIOLATED  -->  receiver-side transfer
\* idempotency IS necessary for the aggregate cap. This is the smallest counterexample.
SPECIFICATION Spec
CONSTANTS
  Replicas = {r1, r2}
  CAP = 2
  Cids = {c1, c2}
  Tids = {t1}
  Amounts = {1, 2}
  SenderIdemp = TRUE
  ReceiverIdemp = FALSE
INVARIANT SafetyLe
