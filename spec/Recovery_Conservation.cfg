\* Safe reclaim, checking CONSERVATION as a transition property. Expect VIOLATED: Lose lowers A
\* (and reclaim raises it) -> conservation fails while Safety (checked in Recovery_Safe) holds.
SPECIFICATION Spec
CONSTANTS Replicas = {r1, r2}  CAP = 3  Amounts = {1, 2}  SafeReclaim = TRUE
PROPERTY ConservedA
