\* Adversarial config: TLC MUST report an invariant violation here (the bug is real).
SPECIFICATION Spec
CONSTANTS
  Replicas = {r1, r2}
  CAP = 2
  Cids = {c1, c2}
  Tids = {t1, t2}
INVARIANT TypeOK
INVARIANT Conserved
INVARIANT Safety
