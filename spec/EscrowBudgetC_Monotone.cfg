\* Positive transition-level check: the CURRENT (recovery-free) protocol is non-creating.
\* Expect HOLD — every step keeps authority A non-increasing (in fact constant = CAP).
SPECIFICATION Spec
CONSTANTS
  Replicas = {r1, r2}
  CAP = 2
  Cids = {c1, c2}
  Tids = {t1}
  Amounts = {1, 2}
  SenderIdemp = TRUE
  ReceiverIdemp = TRUE
PROPERTY Amono
