\* Correct discipline, checking the EXACT conservation equality under crashes. Reported honestly
\* (crashes may DESTROY budget -> Conserved can fail while Safety holds; that is the point).
SPECIFICATION Spec
CONSTANTS
  Replicas = {r1, r2}
  CAP = 2
  Tids = {t1}
  Amounts = {1, 2}
  DebitDurableBeforeSend = TRUE
  RecvdDurable = TRUE
INVARIANT Conserved
